{
  "name": "dashboard-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "vite build --outDir docs",
    "postbuild:pages": "node scripts/ensure-pages-output.mjs",
    "preview": "vite preview",
    "sync:data": "node scripts/sync-finance-data.mjs",
    "update:pages": "npm run sync:data && npm run build:pages"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
